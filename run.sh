# For Docker
# docker-compose up --build -d --remove-orphans --no-deps
# Locally (without VirtualEnv)
python3 -m streamlit run src/streamlit_app.py
# Locally (with VirtualEnv)
# streamlit run src/streamlit_app.py

echo "Finished"